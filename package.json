{
  "name": "firefly-css",
  "version": "0.0.1",
  "description": "CSS Framework with drop in variables",
  "main": "index.js",
  "scripts": {
    "scss": "node-sass --watch scss/ -o dist/",
    "build": "node-sass scss/ -o dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tucker Hawkinson <hawkinson.npm@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "node-sass": "^4.14.1"
  }
}
